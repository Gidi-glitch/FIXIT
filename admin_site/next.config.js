// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow dev server requests from this LAN origin.
  allowedDevOrigins: ['10.27.1.216'],
};

module.exports = nextConfig;
